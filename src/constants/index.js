const HTTP_STATUS = require('./httpStatus');
const ROLES = require('./roles');
const VERDICTS = require('./verdicts');
const LANGUAGE_CONFIG = require('./languages');

module.exports = { HTTP_STATUS, ROLES, VERDICTS, LANGUAGE_CONFIG };
